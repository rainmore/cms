dependencies {
    runtime group: 'mysql',       name: 'mysql-connector-java',        version: project.ext.get('mysql.version')
    implementation group: 'com.zaxxer',  name: 'HikariCP',             version: project.ext.get('hikaricp.version')
}
